[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "greeter"
version = "0.1.0"
description = "Demo gRPC protobuf definitions"
authors = [{ name = "Carl Andersson", email = "github@lillecarl.com" }]
requires-python = ">=3.12"
dependencies = []
