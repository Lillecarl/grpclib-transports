target-version = "py313"
line-length = 120

[lint]
select = [
  "E",
  "W", # pycodestyle
  "F", # pyflakes (real bugs)
  "I", # isort
]

ignore = [
  "E501", # line length handled by line-length setting
]

[lint.isort]
known-first-party = [
  "greeter2",
  "grpclib_transports",
]
