syntax = "proto3";

package demo;

service Greeter {
  rpc SayHello (HelloRequest) returns (HelloReply);
  rpc Upload (stream HelloRequest) returns (HelloReply);
}

message HelloRequest {
  string name = 1;
  bytes payload = 2;
}

message HelloReply {
  string message = 1;
  bytes payload = 2;
}
